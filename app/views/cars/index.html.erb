<h1>All of the Cars</h1>

<ul>
  <% @cars.each do |car| %>
  <li>
    <%= link_to car.make, car_path(car.id) %> - <%= car.model %> - <%= car.year %>
  </li>
  <% end %>
</ul>

<p>
  <%= link_to "Register a New Car", new_car_path %>
</p>
